{
  "name": "agile-metrics-tools",
  "version": "1.0.0",
  "description": "",
  "author": "ferzerkerx",
  "license": "MIT",
  "scripts": {
    "start": "yarn build && yarn prepare-web && node dist/api/server.js",
    "prepare-web": "cp -R src/api/resources dist/ && cp -R src/api/template-configs/* dist/api/template-configs",
    "build": "yarn compile",
    "compile": "tsc",
    "dev": "tsc & nodemon dist",
    "test": "jest --coverage",
    "prettier": "prettier --write --config=.prettierrc \"src/**/*.[tj]s\"",
    "tsc": "tsc",
    "precommit": "lint-staged"
  },
  "dependencies": {
    "@octokit/rest": "^16.13.2",
    "bottleneck": "^2.15.3",
    "csvtojson": "^2.0.8",
    "elasticsearch": "^15.2.0",
    "express": "^4.16.4",
    "node-env-file": "^0.1.8",
    "node-fetch": "^2.3.0"
  },
  "engines": {
    "node": ">=8.9.0"
  },
  "devDependencies": {
    "@types/express": "^4.11.1",
    "@types/jest": "^23.3.3",
    "@types/node": "12.6.8",
    "husky": "^0.14.3",
    "jest": "^23.6.0",
    "lint-staged": "^7.0.0",
    "prettier": "^1.15.2",
    "ts-jest": "^23.10.4",
    "ts-loader": "^5.2.1",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typescript": "^3.1.6"
  },
  "lint-staged": {
    "./**/*.{ts,js,json}": [
      "prettier --write",
      "git add"
    ]
  },
  "jest": {
    "coverageReporters": [
      "lcov"
    ]
  }
}
